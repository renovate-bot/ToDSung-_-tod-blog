# Adversarial Review: specs/ui-library/spec.md + plan.md (v1)

Reviewer stance: find how this fails, not confirm it works. Scope: `specs/ui-library/spec.md`,
`specs/ui-library/plan.md`, cross-checked against `.agents/research/research-ui-stack.md`,
`.agents/research/research-ui-tooling.md`, and repo reality (`package.json` files, `next.config.js`,
`eslint.config.mjs`, `tsconfig*.json`, `.husky/`, `AGENTS.md`). No files modified.

Date of review: 2026-07-13.

---

## 1. Version/fact cross-check (spec.md §3 vs the two research files)

| # | Finding | Location | Failure scenario |
|---|---|---|---|
| 1.1 | `lucide-react` is asserted as "new-york style 預設 icon library" (default icon library) but **neither research file states this**. `lucide` appears exactly once total, in `research-ui-tooling.md:86`, only as part of a third-party password-toggle pattern — not a confirmed shadcn default. | spec.md:50 | An implementing agent trusts a "researched" fact that was never verified; if shadcn CLI 4.13.0's actual default icon library differs (e.g. changed alongside the Base UI default switch noted in research-ui-tooling.md:8), Tier-2 components (PasswordInput) get the wrong import silently. |
| 1.2 | `@shikijs/rehype` is pinned to `^4.3.1`, identical to the `shiki` core version on the same row. `research-ui-tooling.md:31-32` explicitly gives `shiki` version `4.3.1` but leaves `@shikijs/rehype` as "current (matches shiki major)" — i.e. research never pinned a `@shikijs/rehype` version. Spec presents an unresearched number as if it were verified. | spec.md:47 | Agent runs `pnpm add @shikijs/rehype@^4.3.1`; if that exact patch was never published for the rehype package (versioning is independent per-package), install fails or silently resolves to an unintended version. |
| 1.3 | `@storybook/addon-a11y` / `@storybook/addon-vitest` listed with note "隨 SB 10.5" (ships alongside/bundled with SB 10.5), directly under a row (spec.md:38) correctly noting essentials/docs/interactions are core-bundled ("已內建 core，勿另裝"). `research-ui-tooling.md:19-20` states these two addons are explicitly **not** bundled by default — must be installed via `npx storybook add`. The visual parallelism between the two rows invites misreading "隨 SB" as "bundled". | spec.md:39 | Cold-start agent (Phase 1.3) assumes addon-a11y/addon-vitest are already present once `storybook` core is installed, skips the install step, and Storybook fails to load the addons or the acceptance criteria (a11y checks, play tests) silently produce nothing to check. |
| 1.4 | Everything else in spec §3 checked line-by-line against both research files: `tailwindcss@^4.3.2`, `shadcn@4.13.0`, `radix-ui@^1.6.2`, `motion@^12.42.2`, `tw-animate-css@^1.4.0`, `storybook`/`@storybook/react-vite@10.5.x`, `react-hook-form@^7.81.0`, `zod@^4.4.3`, `@hookform/resolvers@^5.4.0` (incl. "Zod 3/4" claim), `@tanstack/react-query@^5.101.2`, `react-markdown@^10.1.0` + `MarkdownHooks` claim, `remark-gfm@^4.0.1`, `rehype-slug`/`rehype-autolink-headings@latest`, `shiki@^4.3.1`, `@tailwindcss/typography@^0.5.20` (incl. v4 peerDep claim), `input-otp@^1.4.2` — all match the research files' stated versions/facts exactly. | spec.md:33-49 vs research-ui-stack.md, research-ui-tooling.md | No mismatch found; checked by direct line-for-line diff against both research tables. |

---

## 2. Internal consistency (spec.md §5 vs plan.md; spec §8 vs plan.md; markdown links)

| # | Finding | Location | Failure scenario |
|---|---|---|---|
| 2.1 | Component coverage: all Tier 1 items (spec.md:83-86, 34 components across 4 groups) map 1:1 to plan.md Phase 2 batches 2.a–2.d (plan.md:37-40); all Tier 2 items (spec.md:90-98) map to Phase 3/4 tasks; all Tier 3 items (spec.md:102) map to Phase 5 tasks. **No component gap found.** | spec.md:81-102 vs plan.md:31-72 | N/A — checked exhaustively by listing both sides and diffing; full coverage confirmed. |
| 2.2 | Markdown links in both files resolve to real files: spec.md:4-5 (`research-ui-stack.md`, `research-ui-tooling.md`, `plan.md`) and plan.md:3 (`spec.md`, `model-dispatch.md`, `delegation-templates.md`) all exist on disk. | spec.md:4-5, plan.md:3 | N/A — verified via Glob; all four target paths resolve. |
| 2.3 | Spec §8 acceptance criterion 2, `pnpm -F @tod-workspace/ui typecheck` (spec.md:121), assumes a package.json script literally named `typecheck`. No package in this repo currently has a `typecheck` script (checked root, tod-blog, leetcode `package.json`), and plan.md Phase 1.1 (plan.md:26) only says "`tsc --noEmit` 過" — it never instructs creating a `"typecheck"` script. | spec.md:121 vs plan.md:26 | A cold-start agent executing Phase 1.1's literal acceptance text runs `tsc --noEmit` directly and calls it done; spec §8.2's exact command (`pnpm -F @tod-workspace/ui typecheck`) then fails at Phase 6.3's full re-run because the script was never added, discovered only at the final gate. |
| 2.4 | Spec §8 acceptance criterion 3, `pnpm -F @tod-workspace/ui storybook:build` (spec.md:122), assumes a script named `storybook:build`. Plan.md only ever says prose "`storybook build` 成功" (plan.md:28, 37-40, 54) — never specifies adding a matching package.json script. Same gap pattern as 2.3. | spec.md:122 vs plan.md:28 | Same as 2.3: literal spec command fails at the final integration gate (Phase 6.3) despite every earlier phase's ad-hoc `storybook build` having "passed". |
| 2.5 | Spec §8.6 ("既有驗證不退步：leetcode 72 tests、articles build") is only explicitly re-verified at Phase 6.3 ("全套驗證：spec §8 六條全跑"). No phase between Phase 1 and Phase 6 re-runs leetcode tests or articles build as a regression checkpoint, despite Phase 1 changing root `eslint.config.mjs`-adjacent shared config (tsconfig/eslint globs) that both leetcode and articles depend on. | plan.md:74-79 (Phase 6) vs plan.md:20-72 (Phases 1-5) | A root-level regression introduced in Phase 1 (e.g. eslint project glob or tsconfig references touching shared root config) is invisible until Phase 6, potentially days/many-commits later, at which point disentangling which of Phases 1-5's commits caused it is expensive. |

---

## 3. Executability (mechanically checkable vs vague acceptance criteria)

| # | Finding | Location | Failure scenario |
|---|---|---|---|
| 3.1 | Phase 5.2 acceptance: "切換動畫流暢、鍵盤操作不破" (transition animation is smooth, keyboard operation isn't broken). No command, metric, or test named — "smooth" is a subjective visual judgment call. | plan.md:70 | Sonnet executor self-certifies "looks smooth to me" with no reviewer-checkable artifact; Phase 5.R reviewer (new context, only given acceptance criteria per model-dispatch §5) has nothing mechanical to re-check and must also eyeball it. |
| 3.2 | Phase 5.3 acceptance: "進出場動畫完整（含 exit）" (enter/exit animation is complete) is paired with checkable items (a11y green, D7 check) but this clause itself has no defined pass/fail test. | plan.md:71 | Same class of problem as 3.1 — partially checkable phase acceptance, partially vibes-based. |
| 3.3 | Phase 3.4 acceptance: "...全部正確渲染" (all render correctly) for a story fed a full GFM/Chinese/multi-language test document — no assertion list (e.g. specific expected DOM nodes, specific rendered table cell count) is defined, only "correctly". | plan.md:52 | The single highest-difficulty task in the whole plan (assigned to opus specifically because of this difficulty) has its core acceptance bar left to reviewer judgment rather than a script-checkable assertion. |
| 3.4 | Phase 3.2 acceptance: "bundle 檢查：shiki 相關 chunk 不含未列語言" (bundle check: shiki-related chunk doesn't contain unlisted languages) — no command specified (no `npx vite-bundle-visualizer`, no build-output grep target, no chunk-naming convention referenced). | plan.md:50 | Windows/PowerShell agent (per AGENTS.md's "no `&&`", no native `grep`/`find`) has to invent tooling for this check from scratch; likely burns retries or skips the check outright. |
| 3.5 | Phase 6.1/6.2 (showcase page completion, owner sign-off) is written as a plain numbered list, not the table-with-驗收-column format every other phase uses; 6.2 explicitly requires owner (human) sign-off, which is correctly *not* agent-checkable, but this format inconsistency makes it easy for an executor to miss that 6.1 has no explicit acceptance line at all beyond "完整化" (make it complete). | plan.md:76-77 | Executor building the final showcase page has no explicit "done" definition distinct from the subjective word "完整" (complete) for task 6.1. |

---

## 4. Contradictions with repo reality

| # | Finding | Location | Failure scenario |
|---|---|---|---|
| 4.1 | **tsconfig composite mismatch.** `tsconfig.base.json:3` sets `"composite": true`, inherited by every package that extends it (confirmed explicitly re-declared in `packages/tod-blog/tsconfig.json:4`). Spec §4.2 states ui's tsconfig will have "驗證用 `tsc --noEmit`，不設 composite 產物" (no composite artifacts) — but neither spec nor plan.md Phase 1.1 instructs explicitly overriding `composite: false` in `packages/ui/tsconfig.json`. Left as-is, `packages/ui` inherits `composite: true` from the base config, contradicting the stated design. | spec.md:76 vs tsconfig.base.json:3, packages/tod-blog/tsconfig.json:4 | Package ends up composite by default despite the spec's explicit claim otherwise; if anyone later runs `tsc --build` at the root, `packages/ui` behaves as a composite project it was never designed to be. |
| 4.2 | **Root project-references graph never updated.** `tsconfig.base.json:21-25` has an explicit `"references"` array listing `packages/articles`, `packages/leetcode`, `packages/tod-blog` — the real mechanism behind AGENTS.md's claim "TypeScript uses project references". Neither spec.md §4.2 nor any plan.md Phase 1 task mentions adding `{ "path": "packages/ui" }` to this array; spec.md:76 only mentions the *ESLint* glob (`packages/*/tsconfig.json`) picking ui up automatically, which is a separate mechanism from the TS project-reference graph. | spec.md:76 vs tsconfig.base.json:21-25 | `packages/ui` is invisible to `tsc --build` at the repo root even though ESLint type-aware parsing does see it — an inconsistency between what type-checks under ESLint and what participates in the declared TS project graph. |
| 4.3 | **ESLint type-aware parsing gap for Storybook config files** — same failure class the repo already hit once and fixed. Root `eslint.config.mjs:20-23` restricts type-aware parsing to `parserOptions.project: ['./packages/*/tsconfig.json']`; `packages/leetcode/eslint.config.mjs:29-33` had to carve out an explicit `disableTypeChecked` exception for `jest.config.ts` because it sits outside that package's tsconfig `include` (`src/`-scoped). Spec's directory layout (spec.md:62) puts `.storybook/` as a **sibling** of `src/`, exactly the same structural position `jest.config.ts` occupied in leetcode before the carve-out was needed (plan.md:17 confirms this was a real bug fixed in Phase 0: "spec/test 檔 type-aware 解析失敗"). Neither spec.md nor plan.md Phase 1.1/1.3 mentions adding the equivalent `disableTypeChecked` block for `.storybook/*.ts` in `packages/ui/eslint.config.mjs`. | spec.md:62 vs eslint.config.mjs:20-23, packages/leetcode/eslint.config.mjs:29-33, plan.md:17 | `npx eslint .` (spec §8.1, the very first acceptance line) fails the moment `.storybook/main.ts`/`preview.ts` are added in Phase 1.3, because those files fall outside `packages/ui/tsconfig.json`'s presumed `src`-only include and type-aware parsing can't resolve them — the exact bug class Phase 0 already spent effort fixing once, about to be reintroduced. |
| 4.4 | Root `package.json:6-8` declares an npm/yarn-style `"workspaces": ["packages/*"]` field alongside the real `pnpm-workspace.yaml`. This is pre-existing repo state, not introduced by spec/plan, and pnpm ignores `package.json#workspaces` in favor of `pnpm-workspace.yaml` — so no functional contradiction, but worth noting spec/plan never mention or account for this redundant/vestigial field when they describe "the workspace" — a cold-start agent unfamiliar with pnpm's precedence rules could be misled into thinking `package.json#workspaces` is authoritative. | package.json:6-8 vs pnpm-workspace.yaml:1-2 | Low risk given pnpm's clear precedence, but flagged since neither reviewed doc acknowledges the duplicate/legacy field exists. |
| 4.5 | Confirmed **no contradiction**: `packages/tod-blog/next.config.js:5` does use `output: 'export'`, matching spec.md §1's "static export" framing and research's static-export-safety claims. `pnpm -F tod-blog build` (spec.md:124, plan.md:27,62) matches the real `build` script in `packages/tod-blog/package.json:7`. `-F @tod-workspace/leetcode` / `-F leetcode` equivalence (AGENTS.md:77) is not contradicted anywhere in spec/plan. | packages/tod-blog/next.config.js:5, packages/tod-blog/package.json:7 | N/A — checked, consistent. |

---

## 5. Missing-risk scan (up to 3, per task instructions — see also §4.3 which is the strongest one)

1. **ESLint type-aware parsing of Storybook/config files outside tsconfig `include`** (detailed in §4.3 above, spec.md:62 / eslint.config.mjs:20-23). This is the single most concrete, repo-grounded risk found: it already happened once (Phase 0 fixed an identical bug for `jest.config.ts` and `**/*.spec.ts`/`**/*.test.ts`), the plan explicitly cites "比照 leetcode 模式" (follow the leetcode pattern) as its template for `packages/ui/eslint.config.mjs` (plan.md:26), yet doesn't carry forward the specific carve-out that made the leetcode pattern work. Because `npx eslint .` clean is both spec §8's *first* acceptance line and a stated "驗證底線" (validation baseline) for every single phase (plan.md:11), this failure would block every Phase-1.3-onward commit under the repo's own pre-commit lint-staged hook, not just fail a final audit.

2. **Windows/PowerShell tooling gaps for prose-only acceptance criteria.** AGENTS.md:60-66 documents this machine has no `&&`/`||`, no native Unix `find`/`grep`/`sed`, UTF-16 LE default file writes, and a hard "stop after two failed retries" rule. Several plan.md acceptance criteria assume some unspecified inspection tooling exists (bundle-content checks in Phase 3.2, plan.md:50; visual "smoothness" in Phase 5.2, plan.md:70) without naming a concrete command. A cold-start sonnet/opus executor on this Windows box, given only the acceptance text (per model-dispatch's "reviewer only gets acceptance criteria + output location" review protocol, plan.md:10), has no specified path to a mechanically reproducible check and is likely to either invent brittle tooling or silently downgrade the check to eyeballing — both of which defeat the plan's own stated goal of "機械可查" (mechanically checkable) acceptance (plan.md:9).

3. **Husky pre-commit hook amplifies risk #1 across every phase.** `.husky/pre-commit:1` runs `npx lint-staged` unconditionally before the leetcode-specific test logic; `.lintstagedrc.json:2` runs `eslint --fix` on every staged `*.{ts,js,tsx,jsx}` file with no package exclusion. Combined with risk #1 (Storybook config files falling outside type-aware parsing scope), and plan.md's own rule that "每個 Phase 至少一個 commit" (plan.md:12), the *entire* multi-week execution plan is exposed to the same commit-blocking failure mode being written into the very first Phase-1.3 commit that introduces `.storybook/`, since husky's hook is not bypassable per AGENTS.md ("不可 `--no-verify`", plan.md:12) without explicit owner approval.

---

## Top 5 (also surfaced in the chat response)

1. §4.3 / §5.1 — ESLint type-aware parsing gap for `.storybook/*.ts` files, same bug class Phase 0 already fixed once for `jest.config.ts`; blocks `npx eslint .` and every pre-commit hook from Phase 1.3 onward. (spec.md:62, eslint.config.mjs:20-23, packages/leetcode/eslint.config.mjs:29-33)
2. §1.1 — `lucide-react` "new-york 預設 icon library" claim is not supported by either research file (invented/unverified fact presented as researched). (spec.md:50)
3. §2.3 / §2.4 — spec §8's literal acceptance commands (`... typecheck`, `... storybook:build`) assume package.json scripts that plan.md never instructs creating; gap only surfaces at the final Phase 6.3 gate. (spec.md:121-122 vs plan.md:26,28)
4. §4.1 / §4.2 — `packages/ui`'s tsconfig will inherit `composite: true` from `tsconfig.base.json` despite spec's explicit "不設 composite 產物" claim, and the root `references` array is never updated to include `packages/ui`. (spec.md:76 vs tsconfig.base.json:3,21-25)
5. §1.3 — `@storybook/addon-a11y`/`addon-vitest` "隨 SB 10.5" phrasing risks being misread as core-bundled when research confirms they require separate install (`npx storybook add`). (spec.md:39 vs research-ui-tooling.md:19-20)

STATUS: done
